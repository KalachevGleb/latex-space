export function startFreeTrial(
  source: string,
  variant?: string,
  segmentation?: Record<string, string>,
  extraSearchParams?: Record<string, string>
) {
  // Non-commercial build: no-op upgrade flow.
  return
}

export function upgradePlan(source: string) {
  // Non-commercial build: no-op upgrade flow.
  return
}
