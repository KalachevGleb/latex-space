// Minimal module definition to enable the 'symbol-palette' feature flag
// and allow frontend components to be imported via Settings.overleafModuleImports.

export default {
  // Optional: declare dependencies if needed in future
}


