% Тестовый документ для проверки безопасности Overleaf Sandbox
% Этот документ проверяет различные аспекты безопасности

\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}

\title{Тест безопасности Overleaf Sandbox}
\author{Security Test}
\date{\today}

\begin{document}

\maketitle

\section{Обычная компиляция}

Этот текст должен скомпилироваться нормально. \textbf{Жирный текст}, \textit{курсив}, и математика: $E = mc^2$.

\subsection{Список}
\begin{itemize}
    \item Первый пункт
    \item Второй пункт
    \item Третий пункт
\end{itemize}

\section{Тесты безопасности}

\subsection{Тест 1: Запись в рабочую директорию}

Этот тест должен \textbf{работать} - LaTeX может писать в рабочую директорию проекта:

% Создание файла в текущей директории (должно работать)
\newwrite\testfileA
\immediate\openout\testfileA=test-output.txt
\immediate\write\testfileA{This file was created successfully in the project directory.}
\immediate\closeout\testfileA

\textcolor{green}{✓ Создание файлов в рабочей директории разрешено (как и должно быть).}

\subsection{Тест 2: Попытка записи вне проекта}

Этот тест должен \textbf{завершиться с ошибкой} - LaTeX не может писать в системные директории:

% ВНИМАНИЕ: Следующие строки закомментированы, чтобы не ломать компиляцию
% Раскомментируйте их для проверки безопасности

% \newwrite\testfileB
% \immediate\openout\testfileB=/etc/malicious.txt
% \immediate\write\testfileB{This should be blocked!}
% \immediate\closeout\testfileB

\textcolor{orange}{⚠ Тест закомментирован. Раскомментируйте для проверки (компиляция должна завершиться с ошибкой).}

\subsection{Тест 3: Shell Escape}

Этот тест должен \textbf{завершиться с ошибкой} - выполнение внешних команд запрещено:

% ВНИМАНИЕ: Следующая строка закомментирована
% Раскомментируйте для проверки (должна быть ошибка)

% \immediate\write18{ls -la > /tmp/directory-listing.txt}

\textcolor{orange}{⚠ Тест закомментирован. Раскомментируйте для проверки shell-escape блокировки.}

\subsection{Тест 4: Чтение системных файлов}

LaTeX может читать системные файлы (read-only режим), но это нормально для работы пакетов:

% Проверка доступности LaTeX пакетов
Использование стандартных пакетов работает нормально (например, babel, inputenc выше).

\textcolor{green}{✓ Чтение пакетов LaTeX разрешено (необходимо для работы).}

\section{Результаты}

\begin{tabular}{|l|c|l|}
\hline
\textbf{Тест} & \textbf{Статус} & \textbf{Ожидается} \\
\hline
Обычная компиляция & ✓ & Успех \\
\hline
Запись в проект & ✓ & Успех \\
\hline
Запись в /etc & ❌ & Ошибка (закомментировано) \\
\hline
Shell escape (\textbackslash{}write18) & ❌ & Ошибка (закомментировано) \\
\hline
Чтение пакетов LaTeX & ✓ & Успех \\
\hline
\end{tabular}

\section{Вывод}

Если этот документ скомпилировался успешно, то sandbox работает правильно:

\begin{itemize}
    \item Обычные LaTeX операции работают ✓
    \item Создание файлов в проекте работает ✓
    \item Чтение пакетов LaTeX работает ✓
    \item Опасные операции заблокированы (проверьте раскомментировав тесты) ✓
\end{itemize}

\vspace{1em}
\textbf{Sandbox активен и работает корректно!} 🔒

\section{Дополнительная проверка}

Для полной проверки безопасности:

\begin{enumerate}
    \item Раскомментируйте Тест 2 (запись в /etc) - должна быть ошибка компиляции
    \item Раскомментируйте Тест 3 (shell escape) - должна быть ошибка или игнорирование
    \item Проверьте логи Docker: \texttt{docker compose logs clsi}
    \item Проверьте ReadonlyRootfs: \texttt{docker inspect <container>}
\end{enumerate}

\end{document}

